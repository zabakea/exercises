"use strict";

const singleName = "Peter";

const capitalT = singleName.substring(2, 3).toUpperCase();

const capi = singleName.substring(0, 2).toLowerCase();

const al = singleName.substring(3);

console.log(`${capi}${capitalT}${al}`);